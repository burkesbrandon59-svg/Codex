# Codex Command Center Dashboard

A lightweight dashboard to monitor and grade command center performance across throughput, reliability, quality, and responsiveness.

## Run locally

Because this project is static HTML/CSS/JS, you can open `index.html` directly in a browser, or serve it locally:

```bash
python -m http.server 8000
```

Then visit <http://localhost:8000>.
